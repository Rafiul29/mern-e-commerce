const registerProcess=require('./register')
const activatedAccount=require('./activatedAccount')
const login=require('./login')
const logout=require('./logout')

module.exports={registerProcess,activatedAccount,login,logout}